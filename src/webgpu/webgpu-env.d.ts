// Pulls in GPUAdapter / GPUDevice / GPU globals for the WebGPU backend.
// Referenced from a .d.ts (rather than tsconfig "types") so the automatic
// inclusion of every other @types package is left untouched.
/// <reference types="@webgpu/types" />
